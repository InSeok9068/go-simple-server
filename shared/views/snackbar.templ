package shared

templ Snackbar() {
	<div
		x-data="$store.snackbar"
		x-show="visible"
		:class="`${type} ${visible ? 'active' : ''}`"
		class="snackbar"
	>
		<span x-text="message"></span>
	</div>
}
