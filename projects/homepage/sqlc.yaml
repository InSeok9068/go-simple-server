version: "2"
sql:
  - engine: "sqlite"
    queries: "homepage-query.sql"
    schema: "homepage-schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
# sqlc generate -f ./projects/homepage/sqlc.yaml
